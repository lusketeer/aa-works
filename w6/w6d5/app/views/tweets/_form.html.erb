<h2>New Tweet!</h2>
<form action="/tweets" class="tweet-compose" data-tweets-ul="#feed" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
    <strong class="chars-left"></strong>
  </label>
  <br>

  <label>
    Mention
    <a href="javascript:void(0)" class="add-mentioned-user">Add User</a>
    <div class="mentioned-users"></div>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>

<script id="user-list" type="text/template">
  <div>
    <a class="remove-mentioned-user" href="javascript:void(0)">Remove Mention</a>
    <select name="tweet[mentioned_user_ids][]">
      <option></option>
      <% User.all.each do |user| %>
      <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
  </div>
</script>
